#!/usr/bin/env python
import random
import time

if __name__ == "__main__":
    random.seed()
    sleeptime = random.randint(0,42)
    print("Sleeping for {0} seconds....".format(sleeptime))
    time.sleep(sleeptime)
    print("I'm awake now!")
